# 🗺️ خارطة طريق وسجل تطوير "المبرمج الثلاثي" (Tri-Coder)

هذا المستند يوثق رحلة تطور التطبيق من البنية التحتية الأساسية وصولاً إلى النسخة الحالية (MVP 2.2)، بالإضافة إلى الخطوات المستقبلية.

## 📁 هيكلة المشروع الحالية (Project Structure)
تم إعادة هيكلة الكود (Refactoring) لتقسيم الملف الضخم إلى وحدات أصغر لسهولة الصيانة:

```text
/
├── index.html          # نقطة الدخول (Entry Point)
├── index.tsx           # ربط React بالـ DOM
├── App.tsx             # المحرك الرئيسي وإدارة الحالة (State Management)
├── constants.tsx       # النصوص (الترجمة) وإعدادات الموديلات
├── types.ts            # تعريفات TypeScript (Interfaces)
├── utils.ts            # دوال مساعدة (توليد ID، تحويل الملفات، التنزيل)
├── db.ts               # طبقة قاعدة البيانات (IndexedDB)
├── Sidebar.tsx         # القائمة الجانبية والإعدادات
├── ChatMessageBubble.tsx # مكون عرض الرسائل (الذكاء الاصطناعي والمستخدم)
├── CodeBlock.tsx       # مكون عرض الأكواد مع التلوين والنسخ
├── PreviewModal.tsx    # نافذة معاينة كود HTML/JS
├── HelpModal.tsx       # (جديد) دليل المستخدم وشرح المزايا
└── ErrorBoundary.tsx   # صائد الأخطاء (Global Error Handler)
```

## 🏁 المرحلة 1: التأسيس والبنية التحتية (Foundation)
**الهدف:** إنشاء قناة اتصال مستقرة مع Gemini API.
- [x] إعداد بيئة العمل (React 19 + TypeScript + Vite/Esbuild).
- [x] دمج مكتبة `@google/genai`.
- [x] تفعيل الـ Streaming Response.

## 🧠 المرحلة 2: هيكلية "الثالوث الذكي" (The Trinity Architecture)
**الهدف:** تحويل التطبيق من "شات بوت" عادي إلى "فريق عمل".
- [x] تعريف الشخصيات الثلاث (Logic Master, Code Ninja, Mentor).
- [x] بناء نظام التبويبات (Tabs UI).

## 💾 المرحلة 3: الذاكرة وإدارة البيانات (Persistence Layer)
**الهدف:** الحفاظ على بيانات المستخدم محلياً.
- [x] بناء `dbHelper` (IndexedDB).
- [x] نظام المجلدات (Folders).
- [x] التصدير والاستيراد (Backup System).

## ⚖️ المرحلة 4: نظام الحكم والإجماع (Consensus Engine)
- [x] ميزة "المقارنة" (Judge).
- [x] ميزة "الإجماع" (Consensus).

## 🎨 المرحلة 5: تحسين التجربة (UI/UX) - *الحالة الحالية*
- [x] دعم كامل للغة العربية (RTL).
- [x] نظام الثيمات (Dark/Light).
- [x] تقسيم الكود (Code Splitting).
- [x] **جديد:** دليل المستخدم (حول التطبيق).
- [x] **جديد:** نظام الإبلاغ عن الأخطاء (Bug Reporting).

## 🚀 المرحلة 6: المستقبل (Next Steps - V3.0)
- [ ] **Voice Conversation:** محادثة صوتية مستمرة.
- [ ] **Image Analysis:** تحليل الصور والمخططات.
- [ ] **توسع هيكلية الذكاء الاصطناعي (Advanced Model Architecture):**
    - **شخصيات تخصصية (New Personas):** إضافة "خبير أمن سيبراني" (Security Auditor)، "مصمم واجهات" (UI/UX Designer)، و"مهندس اختبارات" (QA Engineer).
    - **تعدد المحركات (Multi-Provider Support):** دمج نماذج قوية أخرى مثل **Claude 3.5 Sonnet** و **DeepSeek Coder** و **OpenAI GPT-4o**.
    - **النماذج المحلية (Local LLMs):** دعم الربط مع **Ollama** لتشغيل نماذج مفتوحة المصدر (مثل Llama 3) محلياً للخصوصية التامة والعمل بدون إنترنت.